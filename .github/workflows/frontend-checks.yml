name: Frontend Checks

on:
  pull_request:
    paths:
      - 'src/frontend/**'

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Prepare
        uses: ./.github/actions/prepare
      - name: Lint
        run: npm run check:unused:svelte

  may-merge:
    needs: ['check']
    runs-on: ubuntu-latest
    steps:
      - name: Cleared for merging
        run: echo OK
