/* eslint-disable require-await, no-console */

export const testMathRandom = async () => {
	console.log('Random:', Math.random());
	console.log('Random:', Math.random());
	console.log('Random:', Math.random());
};

/* eslint-enable */
