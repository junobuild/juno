/* eslint-disable require-await, no-console */

export const testMathRandom = async () => {
	console.log('One:', Math.random());
	console.log('Two:', Math.random());
	console.log('Three:', Math.random());
};

/* eslint-enable */