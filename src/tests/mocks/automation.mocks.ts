import {
	type ConsoleActor,
	idlFactoryObservatory,
	type ObservatoryActor,
	type SatelliteActor,
	type SatelliteDid
} from '$declarations';

export const mockRepositoryKey: SatelliteDid.RepositoryKey = {
	owner: 'junobuild',
	name: 'juno'
};